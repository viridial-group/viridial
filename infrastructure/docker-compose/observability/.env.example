# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_grafana_admin_password

# PostgreSQL Connection (for postgres-exporter)
# These should match the values from the base services .env
POSTGRES_USER=viridial
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=viridial

# Alertmanager SMTP (optional - uncomment when needed)
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_USER=alerts@viridial.com
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=alerts@viridial.com

# Slack Webhook (optional - uncomment when needed)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
