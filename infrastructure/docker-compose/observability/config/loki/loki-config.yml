# Loki minimal config compatible avec l'image actuelle

auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2020-10-24
      store: boltdb-shipper
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

ruler:
  alertmanager_url: http://alertmanager:9093

limits_config:
  # Désactive les structured metadata pour rester compatible avec schema v11 + boltdb-shipper
  allow_structured_metadata: false
  retention_period: 720h  # 30 jours
  max_query_length: 0h
  max_query_parallelism: 32

compactor:
  working_directory: /loki/compactor
  compaction_interval: 10m

# loki 
auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2020-10-24
      store: boltdb-shipper
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

ruler:
  alertmanager_url: http://alertmanager:9093

# Retention
limits_config:
  retention_period: 720h  # 30 days 
  max_query_length: 0h
  max_query_parallelism: 32

# Compactor
compactor:
  working_directory: /loki/compactor
  compaction_interval: 10m

