---
# Service Meilisearch pour Production 
apiVersion: v1
kind: Service
metadata:
  name: meilisearch
  namespace: viridial-production
  labels:
    app: meilisearch
spec:
  type: ClusterIP
  ports:
  - port: 7700
    targetPort: 7700
    protocol: TCP
    name: http
  selector:
    app: meilisearch
