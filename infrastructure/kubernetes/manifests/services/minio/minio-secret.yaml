---
# Secret pour MinIO Production 
apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: viridial-production
type: Opaque
stringData:
  MINIO_ROOT_USER: minioadmin
  MINIO_ROOT_PASSWORD: "" # À remplacer par un mot de passe sécurisé (minimum 8 caractères)
